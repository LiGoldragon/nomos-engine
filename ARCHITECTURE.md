# nomos-engine architecture
A real daemon and thin CLI. Signal → Nexus → SEMA Kameo actors fetch TypeSchema Core from central Sema, apply the real typed `MacroPackage::wire_fixture`, persist the package identity, and persist resulting EncodedLogos through typed binary messages.
