# nomos-engine architecture
A real daemon and thin CLI. Signal → Nexus → SEMA Kameo actors fetch an Encoded TypeSchema from central Sema, apply the real typed `MacroPackage::enriched_fixture` after definition-time validation of the closed realize/splice escape set, persist the package identity, and persist resulting EncodedLogos through typed binary messages.
